sudo: false

language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - python-docutils

script:
  - CI/check_codestyle.sh
  - export CFLAGS="-Wall"
  - autoreconf -fvi && ./configure && make distcheck
